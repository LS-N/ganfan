# 待办积压

## BACKLOG-001 场景字段 Phase 5 接入 AI 个性化建议

- **来源**：2026-05-27
- **说明**：分析页「场景」字段（外卖/堂食/自己做/食堂/路边摊等）目前仅做记录和菜系地图统计，不影响 AI Prompt。Phase 5 算法成熟后打通：同一道菜在不同场景（如食堂 vs 外卖）给出不同饮食建议。
- **当前状态**：未开始，等 Phase 5 排期。

## BACKLOG-002 营养类别高低判断引入科学阈值规则

- **来源**：2026-05-27
- **说明**：当前 `stapleLevel / proteinLevel / vegetableFiberLevel / oilLevel / portionLevel` 的高/中/低完全由视觉 AI 模型目测给出，无数值阈值。后期需建立一套基于科学参考值的判断规则，结合两类用户数据：
  - **基础数据**（Profile）：性别、体重、身高、年龄、每日预算、目标（减脂/增肌/维持）
  - **身体结构数据**（Body Puzzle）：通过反馈积累的 `diet_structure_type`，如碳水敏感、蛋白稳定等
  - 规则示例：蛋白质 > `体重(kg) × 1.2g` 为 high；单餐碳水 > 当日预算 × 0.4 为 high；油脂 > 25g 为 heavy 等
  - AI 估算的 `nutrition.protein/carbs/fat` 数值 + 营养库回填值，共同驱动等级字段，覆盖 AI 原始输出
- **当前状态**：未开始。触发条件：Phase 1 完成、Body Puzzle 数据积累到 mature 阶段（≥21 餐反馈）后优先排期。

---

## BACKLOG-003 【上线红线】AI 调用成本上限 + 硬熔断

- **来源**：2026-06-03
- **说明**：实现 `docs/architecture/observability.md` Phase 1 最小闭环第 1-2 项：①每笔 AI 调用写入 `ai_call_log` 表（user_id / use_case / provider / token 数 / cost_cny / status）；②平台日成本超 ¥200 自动硬熔断 + 推送报警。熔断逻辑必须是纯规则代码，不经过任何 LLM。
- **当前状态**：未开始。**Phase 1 真实联调时同步实现，不能上线后补。**

## BACKLOG-004 【上线红线】错误报警 Webhook

- **来源**：2026-06-03
- **说明**：任何 5xx / 未捕获异常推送到飞书或钉钉 webhook（环境变量 `ALERT_WEBHOOK_URL`）。三级预警：🔴 危险才推送用户，🟡 预警由 Agent 自动处理，🟢 健康静默。同一错误 5 分钟内去重。Phase 1 上线前必须配好并验证一次推送成功。
- **当前状态**：未开始。**Phase 1 真实联调时同步实现，不能上线后补。**

## BACKLOG-005 【上线红线】用户行为埋点（含分层三指标）

- **来源**：2026-06-03
- **说明**：按 `docs/architecture/observability.md` + 蓝图用户分层要求埋三个分层验证指标：①`daily_photo_count`（轻度量化派活跃度）②`delivery_click_count`（重度托管派验证信号）③`health_auth_rate`（健康极客触达率）。同时埋转化漏斗关键事件（第 1/3/7 天钩子触发、付费墙触达、订阅转化）。写入 `fulfillment_events` 表。
- **当前状态**：未开始。**Phase 1 真实联调时同步实现，不能上线后补。**

## BACKLOG-006 【上线后第 1 周】验证 OTA 热更新链路

- **来源**：2026-06-03
- **说明**：验证 EAS Update 的 JS 层热更新链路完整可用：推一个小改动 → 真机自动收到更新 → 无需重新打包。这是紧急 bug 快速修复的核心能力，关系到上线后能否"快速修正"。
- **当前状态**：未开始。上线后第 1 周执行。

## BACKLOG-007 【上线后第 1 周】盈亏监测每日定时 Agent

- **来源**：2026-06-03
- **说明**：实现 `docs/business/break-even-monitoring.md` 的数据 Agent 每日自动跑：汇总 MRR / 月总成本 / 盈亏覆盖率 / 单付费用户毛利 / 免费用户成本占比 / 付费转化率。复用现有「营养库巡检」定时机制。覆盖率 < 80% 触发 🔴 推送。
- **当前状态**：未开始。上线后第 1 周执行。

## BACKLOG-008 【上线后第 1 个月】QA Agent

- **来源**：2026-06-03
- **说明**：建立第一个开发 sub-agent：`.claude/agents/ganfan-qa.md`。职责：每次代码变更后自动跑 lint / typecheck / test / 验收清单，把质量门自动化。按 `docs/architecture/agent-architecture.md` 的标准定义格式写 frontmatter。这是验证 orchestrator-worker 模式的第一步。
- **当前状态**：未开始。上线后第 1 个月执行，优先级高于其他 sub-agent。

## BACKLOG-009 【上线后第 1 个月】补跨模块集成测试

- **来源**：2026-06-03
- **说明**：补关键跨模块链路的集成测试。重点两条：①拍照 → AI 分析 → 写入 `meal_analysis` → 触发 `deriveInsights` → 影响抽卡池；②反馈提交 → `comfort` 字段 → 身体拼图解锁阈值。现有单元测试覆盖不了这类链路 bug。
- **当前状态**：未开始。上线后第 1 个月执行。

## BACKLOG-010 【Phase 2 启动前】托管饮食档（④）最小原型方案

- **来源**：2026-06-03
- **说明**：按蓝图「用户分层与产品形态对应」工程约束：Phase 2 启动门控前必须出 ④ 托管饮食的最小可点击原型（轻量接外卖平台 API，¥128/月）。这是验证「愿意外包饮食」核心商业假设的最便宜探针——验证失败则整个商业模式需重审，不能等到 Phase 5 才发现。
- **当前状态**：未开始。Phase 2 启动门控前必须完成原型设计并用户确认。

## BACKLOG-011 【Phase 1 上线后】cost-model.md 双栏修正（P2 F11）

- **来源**：2026-06-03（##044/##045 遗留）
- **说明**：`docs/business/cost-model.md` 当前假设「AI 团队全栈自动化（人力=¥0）」，但实际当前态是你手动操作。需加双栏对比：「理想态（Agent 全栈）」vs「当前态（手动）」，避免成本预测过于乐观。
- **当前状态**：未开始。Phase 1 上线后补，不阻塞主线开发。
