# 流程 NN: {流程名}

> 复制本模板新建文档时：
> 1. 文件名格式 `NN-flow-name-en.md`
> 2. 在 `README.md` 的流程清单中注册
> 3. 填完每一节，不允许 N/A 占位（如不适用，写明原因）

## 一、流程基本信息

| 字段 | 内容 |
|---|---|
| 流程名 | |
| 触发 | （什么用户动作 / 系统事件触发） |
| 涉及页面 | （s-xxx → s-yyy） |
| 用户耗时 | （估算秒/分钟） |
| 入口动作 ID | （对应 phase-1-action-data-layer-map.md 的圈号） |
| 出口动作 ID | |
| 频率估计 | （每天/每餐/不定） |

## 二、四层泳道图（核心）

按时间顺序逐步填表。每行 = 一个用户可感知或系统自动的步骤。

| # | USER | AI 生产 | Layer 1 写入 | Layer 2 触发 | Layer 3 渲染 |
|---|---|---|---|---|---|
| 1 | | | | | |
| 2 | | | | | |

**填写规则：**
- USER 列：用户的具体动作（点击 X / 输入 Y / 等待）
- AI 生产列：AI 调用的函数名 + 输入/输出，没有则填 `—`
- Layer 1 写入列：写哪张表的什么字段，没有则填 `—`
- Layer 2 触发列：是否调用 deriveInsights 重算 / 读取，没有则填 `—`
- Layer 3 渲染列：UI 变化（哪个函数 / 哪个组件刷新），没有则填 `—`

## 三、数据血缘

| 写入字段 | 来源 | 被哪些消费者消费 |
|---|---|---|
| | | |

只列本流程新写入的字段。来源可以是「用户输入」「AI 生产」「系统计算」。消费者必须列具体函数名或页面。

## 四、触发条件

| 类型 | 条件 |
|---|---|
| 前置 | （进入这个流程必须满足什么） |
| 防御 | （前端/后端的校验逻辑） |
| 降级 | （前置不满足时怎么处理） |

## 五、洞察影响

### 读取的 insights
- （列出本流程会从 deriveInsights 读取哪些 angle / category / maturity 子集）

### 写入触发的可能洞察变化
- （本流程的 L1 写入可能影响哪些 insight 的 sampleSize / hitRate / maturity）
- （列出可能的状态变迁：sparse→forming / forming→mature）

## 六、架构检查清单

对照三层架构和数据复利飞轮原则做自检：

- [ ] 所有 L1 写入都是 append-only（不更新旧条）
- [ ] L2 重算保持幂等（不持久化中间结果）
- [ ] L3 消费层从 L2 取数（不绕过引擎自己算规律）
- [ ] AI 调用如属消费者，prompt 中引用了 insights 上下文（不让 AI 自由编造）
- [ ] 弹窗 / 触发类去重通过 localStorage 永久标记
- [ ] 数据血缘可追溯（sourceRefs 等字段完整）
- [ ] 没有引入新的"小算法"孤岛

任何 [✗] 都必须在「已知架构债」节说明。

## 七、已知架构债（如有）

- （列出本流程当前不符合架构原则的地方 + 修复优先级 P0/P1/P2 + 关联 TASK_LOG 编号）

## 八、变更记录

- YYYY-MM-DD 初版（关联 ##NNN 任务）
- YYYY-MM-DD 修订说明
