# Phase 3 / MVP 3.0 目标干预验收清单

来源：`docs/02-master-blueprint.md` 第 2391-2479 行。

任何 Phase 3 任务结束前，必须逐条标记 `PASS` / `PARTIAL` / `FAIL` / `N/A`，并写入 `docs/TASK_LOG.md`。

## 最终验收

- [ ] `body_patterns` 表有数据，相关性置信度 >0.6。
- [ ] 规律可视化界面数据准确。
- [ ] 断食模块可正常使用，计时准确。
- [ ] 计划生成排除了已知负向关联菜品。

## 任务级验收

| 任务 | 验收项 |
|---|---|
| T11-01 | 注入已知相关数据后检测出正确相关性；`body_patterns` upsert；version 递增 |
| T11-02 | 第 21 条带回访记录提交后，满足条件异步生成 `body_patterns` |
| T11-03 | 规律列表、置信度、样本数与数据库一致 |
| T12-01 | 不同用户特征对应不同断食推荐方案 |
| T12-02 | 断食计时准确；开始/结束可用；App 重启后状态恢复 |
| T12-03 | 断食中拍照弹窗出现；选 A 结束断食并保存；选 B 保存但断食状态不变；非断食无额外步骤 |
| T13-01 | 体重连续 3 周上升时热量目标自动降低 |
| T13-02 | 注入连续超标数据后 `interventions` 有记录，首页提示出现 |
| T14-01 | 有“辛辣 -> 肠胃不适”规律的用户，计划中无辛辣菜 |

## 必跑命令

```powershell
npm run lint
npm run typecheck
npm test
```
