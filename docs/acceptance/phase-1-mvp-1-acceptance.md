# Phase 1 / MVP 1.0 记录感知验收清单

来源：`docs/02-master-blueprint.md` 第 1920-2282 行。

任何 Phase 1 任务结束前，必须逐条标记 `PASS` / `PARTIAL` / `FAIL` / `N/A`，并写入 `docs/TASK_LOG.md`。

## 最终验收

- [ ] 完整走通：注册 -> 建档 -> 拍照 -> 分析 -> 反馈 -> 回访。
- [ ] 7 天连续记录后首页出现身体洞察。
- [ ] 断网可记录，联网后数据完整同步。
- [ ] Android 真机稳定运行 24 小时无崩溃。
- [ ] `meals` 表数据完整率 > 95%，所有字段无意外 null。

## Sprint 1 验收

| 任务 | 验收项 |
|---|---|
| T1-01 | Supabase Dashboard 中存在所有 1.0 表；RLS 策略已启用 |
| T1-02 | 能注册新账户；登录后跳转主界面；token 持久化 |
| T1-03 | 首次登录进入建档流程；完成后 `profiles` 表有数据 |
| T1-04 | App 启动时 SQLite 数据库自动创建；表结构与 Supabase 一致 |
| T1-05 | `nutrition_items` >=1000 条；embedding 非空；红烧肉精确查询有结果；东坡肉向量搜索命中红烧肉且 score >=0.8 |

Sprint 1 总验收：能注册登录，能填写个人档案，数据同时写入 SQLite 和 Supabase，营养向量库已初始化。

## Sprint 2 验收

| 任务 | 验收项 |
|---|---|
| T2-01 | `uvicorn main:app --reload` 在本地 8000 端口启动 |
| T2-02 | curl 分析接口 response 包含 `nutrition_source`；红烧肉命中向量库；生僻菜品未命中时正常返回；营养搜索接口返回 top5 |
| T2-03 | 能拍照，图片显示在预览区 |
| T2-04 | 压缩后图片 <100KB；`meal_images` 有记录；`image_type` 字段正确 |
| T2-05 | 分析结果正确显示；营养来源标签根据 source 显示；修改后 `meal_corrections` 有记录 |
| T2-06 | 餐次数据在 `meals` 和 `meal_analysis` 两表均有记录 |
| T2-07 | `meal_started_at` 有值；超 30 分钟未反馈后 `auto_closed_at` 有值；首页仍显示待反馈卡 |
| T2-08 | 加料内容写入 `additionals` 字段；详情页能显示 |
| T2-09 | 选了心情后 `meals.mood` 有值；跳过为 null 且不影响保存 |

Sprint 2 总验收：拍照 -> AI 分析 -> 看到结果 -> 可选加料/心情 -> 保存，全流程 <30 秒，数据完整写入。

## Sprint 3 验收

| 任务 | 验收项 |
|---|---|
| T3-01 | 4 题全答完才能提交；数据写入 `meal_feedback` 正确 |
| T3-02 | 单元测试覆盖早饭/午饭/加餐/晚饭 dueAt 计算和同日多餐合并 |
| T3-03 | 回访卡到时出现；提交后消失；`daily_checkins` 写入；`meal.daily_checkin_id` 有值 |
| T3-04 | Android 真机收到即时反馈通知；点击跳转 feedback 页面 |
| T3-05 | 指定时间 Android 真机收到每日回访通知 |

Sprint 3 总验收：完整走通一餐：拍照 -> 分析 -> 即时反馈 -> 当天/次日回访，数据全链路完整。

## Sprint 4 验收

| 任务 | 验收项 |
|---|---|
| T4-01 | 能看到当月记录；点击日期展开当天餐次列表 |
| T4-01b | 记录 2 个不同省份菜系后地图对应省份高亮；点击显示餐次列表 |
| T4-02 | 所有已记录字段正确显示，无 null 崩溃；未反馈可补录 |
| T4-03 | 筛选后列表和日历同步更新 |
| T4-04 | `weight_logs` 有时序数据，趋势计算正确；超过 7 天未录入出现提醒 |
| T4-05 | 所有档案字段可编辑，修改后 `profiles` 更新 |
| T4-06 | 清空后 `meals` 表为空；注销后 auth session 失效 |

Sprint 4 总验收：能找到任意历史餐次完整数据，体重有时序记录，档案可完整编辑。

## Sprint 5 验收

| 任务 | 验收项 |
|---|---|
| T5-01 | 单元测试覆盖 5 条规则和边界情况 |
| T5-02 | 推荐卡接口返回正确格式，无相同菜系重复 |
| T5-03 | 接受/跳过后 `card_actions` 有完整记录；badge/risk 与展示一致；刷新后不重复展示 |
| T5-04 | DEFAULT/PENDING_FB/DONE/CHECKIN_DUE 四种状态切换正确，不错乱 |
| T5-05 | 有 7 天数据的测试账号首页出现洞察卡片 |
| T5-06 | 满 7 餐后身体拼图区域出现；各块按条件独立显示/隐藏；美食地图按钮跳转正确；数据准确无 null 崩溃 |

Sprint 5 总验收：首页逻辑完整，推荐卡功能可用，7 天后出现首条身体洞察。

## Sprint 6 验收

| 任务 | 验收项 |
|---|---|
| T6-01 | 断网记录 3 餐，联网后 Supabase 有完整数据 |
| T6-02 | 关闭 WiFi 出现横幅，开启 WiFi 横幅消失 |
| T6-03 | 关闭 AI 服务后拍照出现错误界面，重试/换图/模拟结果继续均可用 |
| T6-04 | 100 条历史记录滚动流畅，首屏加载 <2 秒 |
| T6-05 | 手动触发测试错误后 Sentry Dashboard 有记录 |
| T6-06 | `eas build --profile preview` 构建成功，APK 可真机安装 |
| T6-07 | 5 台测试设备安装运行，无崩溃 |

## 必跑命令

```powershell
npm run lint
npm run typecheck
npm test
```

涉及原生能力、通知、相机、SQLite、EAS 或真机体验时，必须补充真机/构建/外部服务验收。
